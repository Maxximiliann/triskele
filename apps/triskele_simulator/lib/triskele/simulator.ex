defmodule Triskele.Simulator do
  @moduledoc """
  Kraken-mimicking exchange simulator for tests and development.

  Provides a deterministic, in-process mock exchange that speaks the same
  protocol as the real Kraken API. Used for integration tests, chaos scenarios,
  and end-to-end backtest replays.

  Implemented in Phase 4+.
  """
end
