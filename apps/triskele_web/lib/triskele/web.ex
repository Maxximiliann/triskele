defmodule Triskele.Web do
  @moduledoc """
  Phoenix LiveView dashboard for single-operator control.

  Provides real-time visibility into order book state, active cycles,
  portfolio balances, P&L, and system health. Exposes operator controls
  for the kill switch, runtime parameter overrides, and trade journal queries.

  Implemented in Phase 6.
  """
end
