defmodule Triskele.KrakenClient do
  @moduledoc """
  REST and WebSocket adapters for the Kraken exchange API.

  Owns all parsing, validation, and error normalization for Kraken responses.
  No raw HTTP responses or WebSocket frames flow past this boundary.

  Implemented in Phase 1.
  """
end
