defmodule Triskele.Backtest do
  @moduledoc """
  Historical replay harness using Tardis.dev market data.

  Replays historical Kraken order book snapshots and incremental updates
  through the full engine and execution stack, producing a ground-truth
  P&L oracle for strategy validation.

  Implemented in Phase 5.
  """
end
